# Optional: Switch to your conda or virtual env first
# install naoth via pip by running the following command in /Utils/py

# on the gruenau servers use: pip2 install --user -e naoth

pip install -e naoth
