# mounts the stick and start the systemd service which will run the brainwashing script

KERNEL=="sd*", SUBSYSTEMS=="usb", ENV{ID_FS_TYPE}=="vfat", ENV{ID_FS_LABEL}!="*logstick*||*LOGSTICK*", ACTION=="add", SYMLINK+="brainwasher", ENV{SYSTEMD_WANTS}+="brainwasher"
