TODO:
Move this to boosterimage repo when it has been created.
It is cursed to have this here in the repo.

The name Ansible comes from the Ansible tool, which manages the state of servers (i.e. assures that certain files are at the right place)
We do not (yet) use Ansible for this, but the name still makes sence.

For now, we deploy by copying all the files over to the booster with a brainwash.sh DeployStick.

A .alibi file is used to assue the directory is added to git and created on the robot.
