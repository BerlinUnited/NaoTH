/** Kick with foot tester */
option simple_penalty_kicker_agent {

  initial state test {    
    action {
    
      // react to the game controller and handle fall down
      basic_play();

      kick_with_foot_test();//ersetzen durch  kick_long_with_approach_right(); oder kick short
      
      // control the arms
      arms_control();

      // control head movement according to value of head.control_mode
      head_control();
    }
  }
}
 
