/** Kick with foot tester */
option simple_penalty_kicker {

  initial state test {    
    action {
    
      // react to the game controller and handle fall down
      basic_play();

      kick_with_foot_test();
      
      // control the arms
      arms_control();

      // control head movement according to value of head.control_mode
      head_control();
    }
  }
}
 
